services:
  database:
    image: postgres:15
    container_name: military_tech_database

    ports:
      - "5436:5432"

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: military_tech

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: